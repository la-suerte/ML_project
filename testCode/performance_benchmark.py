# -*- coding: utf-8 -*-
"""performance_benchmark

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1bjc2oeOybsGvcWj9SZYu3Vgp8pDrqmKy
"""

import time
import numpy as np
import platform
import os

def run_svd_benchmark(matrix_size=1000):
    """
    Teste la performance de la machine spécifiquement sur les calculs matriciels
    utilisés dans le projet (SVD et Multiplication).
    """
    print(f"\n{'='*50}")
    print("BENCHMARK DE PERFORMANCE (COMPUTE POWER TEST)")
    print(f"{'='*50}")

    print(f"Système : {platform.system()} {platform.release()}")
    print(f"Processeur : {platform.processor()}")
    print(f"Architecture : {platform.machine()}")

    print(f"\n1. Génération d'une matrice aléatoire ({matrix_size}x{matrix_size})...")
    start_gen = time.time()
    data = np.random.rand(matrix_size, matrix_size)
    end_gen = time.time()
    print(f"   -> Fait en {end_gen - start_gen:.4f} secondes.")

    print("\n2. Test Multiplication Matricielle (Covariance simulée)...")
    start_dot = time.time()
    cov_matrix = np.dot(data.T, data)
    end_dot = time.time()
    print(f"   -> Fait en {end_dot - start_dot:.4f} secondes.")

    print("\n3. Test SVD (Singular Value Decomposition)...")
    start_svd = time.time()
    u, s, vh = np.linalg.svd(cov_matrix)
    end_svd = time.time()
    svd_time = end_svd - start_svd

    print(f"   -> Fait en {svd_time:.4f} secondes.")

    print(f"\n{'='*50}")
    if svd_time < 1.0:
        print("VERDICT : PERFORMANCE EXCELLENTE 🚀")
        print("Cette machine peut gérer de gros datasets sans problème.")
    elif svd_time < 5.0:
        print("VERDICT : PERFORMANCE CORRECTE ✅")
        print("Suffisant pour les tests actuels.")
    else:
        print("VERDICT : PERFORMANCE FAIBLE ⚠️")
        print("Il faudra peut-être réduire la taille des historiques pour l'entraînement.")
    print(f"{'='*50}")

if __name__ == "__main__":
    run_svd_benchmark(matrix_size=1500)